{
  "name": "genz-deezer",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "web": "pnpm --filter web dev",
    "backend": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "22.13.1",
    "typescript": "5.6.2"
  }
}
